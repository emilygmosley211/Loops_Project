To compile and run the C++ program, open the terminal in GitHub CodeSpaces. You can click View > Terminal, or use Ctrl + ~ (Windows) or Cmd + ~ (Mac)
Run the following command to compile the program.  
  g++ -o loops_project loops_project.cpp
Run the following command to execute the compiled program.
  ./loops_project
